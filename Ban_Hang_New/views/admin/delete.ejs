
<div class="container mt-5">
  <div class="card shadow p-4">
    <h2 class="text-center text-danger">🗑️ Xóa sản phẩm</h2>

    <p class="mt-3">Bạn có chắc chắn muốn xóa sản phẩm sau không?</p>

    <ul>
      <li><strong>Tên:</strong> <%= product.name %></li>
      <li><strong>Giá:</strong> <%= product.price %> ₫</li>
      <li><strong>Mô tả:</strong> <%= product.description || '—' %></li>
      <% if (product.imageUrl) { %>
        <li><strong>Ảnh:</strong> <br>
          <img src="/uploads/<%= product.imageUrl %>" width="150" class="rounded mt-2">
        </li>
      <% } %>
    </ul>

    <form action="/admin/delete/<%= product.id %>?_method=DELETE" method="POST" class="mt-3">
      <button type="submit" class="btn btn-danger">Xác nhận xóa</button>
      <a href="/admin" class="btn btn-secondary">Hủy</a>
    </form>
  </div>
</div>
